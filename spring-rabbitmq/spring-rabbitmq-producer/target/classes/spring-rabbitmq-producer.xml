<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/rabbit
       http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">
    <!--加载配置文件-->
    <context:property-placeholder location="classpath:rabbitmq.properties"/>
    <!--定义rabbitmq connectionFactory-->
    <rabbit:connection-factory id="connectFactory" host="${rabbitmq.host}"
                               port="${rabbitmq.port}"
                               username="${rabbitmq.username}"
                               password="${rabbitmq.password}"
                               virtual-host="${rabbitmq.virtual.host}"/>
    <!--定义管理交换机、队列封装成了admin-->
    <rabbit:admin connection-factory="connectFactory"/>
    <!--定义持久化队列，不存在则自动创建；不绑定到交换机则绑定到默认交换机
       默认交换机类型为Direct，名字为：""，路由健为队列的名字
    -->
    <rabbit:queue id="spring_queue" name="spring_queue" auto-declare="true"/>

    <!--~~~~~~~~~~~~~~~~~~~~pub/sub广播所有队列都能收到~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~-->
    <!--定义广播交换机中的持久化队列，不存在则自定创建-->
    <rabbit:queue id="spring_fanout_queue1" name="spring_fanout_queue" auto-declare="true"/>
    <rabbit:queue id="spring_fanout_queue2" name="spring_fanout_queue" auto-declare="true"/>
    <!--定义广播类交换机，并绑定上述两个队列-->
    <rabbit:fanout-exchange id="spring_fanout_exchange" name="spring_fanout_exchange" auto-declare="true">
        <rabbit:bindings>
            <rabbit:binding queue="spring_fanout_queue1"/>
            <rabbit:binding queue="spring_fanout_queue2"/>
        </rabbit:bindings>
    </rabbit:fanout-exchange>

    <!--~~~~~~~~~~~~~~~~~topic通配符：*匹配一个单词，#匹配0个或多个单词~~~~~~~~~~~~~~~~~~~~~-->
    <rabbit:queue id="spring_topic_queue1" name="spring_topic_queue1" auto-declare="true"/>
    <rabbit:queue id="spring_topic_queue2" name="spring_topic_queue2" auto-declare="true"/>
    <rabbit:queue id="spring_topic_queue3" name="spring_topic_queue3" auto-declare="true"/>
    <rabbit:topic-exchange name="spring_topic_exchange" id="spring_topic_exchange" auto-declare="true">
        <rabbit:bindings>
            <rabbit:binding pattern="example.*" queue="spring_topic_queue1"/>
            <rabbit:binding pattern="example.#" queue="spring_topic_queue2"/>
            <rabbit:binding pattern="goods.#" queue="spring_topic_queue3"/>
        </rabbit:bindings>
    </rabbit:topic-exchange>

    <!--~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~routing~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~-->
    <rabbit:queue id="spring_direct_queue1" name="spring_routing_queue1" auto-declare="true"/>
    <rabbit:queue id="spring_direct_queue2" name="spring_routing_queue2" auto-declare="true"/>
   <rabbit:direct-exchange name="spring_direct_exchange" id="spring_direct_exchange" auto-declare="true">
       <rabbit:bindings>
           <rabbit:binding key="error" queue="spring_direct_queue1"/>
           <rabbit:binding key="info" queue="spring_direct_queue2"/>
       </rabbit:bindings>
   </rabbit:direct-exchange>
    <!--定义rabbitTemplate对象操作可以在代码中方便发送消息-->
    <rabbit:template id="rabbitTemplate" connection-factory="connectFactory"/>
</beans>